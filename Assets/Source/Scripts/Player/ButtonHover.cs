using UnityEngine;
using UnityEngine.UI;
using TMPro;
using UnityEngine.EventSystems;

public class ButtonHover : MonoBehaviour//, IPointerEnterHandler, IPointerExitHandler
{
    /*public TextMeshProUGUI text;
    public Color normalColor1 = Color.white;
    public Color normalColor2 = Color.white;
    public Color hoverColor1 = new Color32(251, 170, 128, 255);
    public Color hoverColor2 = new Color32(249, 191, 115, 255);

private TMP_ColorGradient normalGradient;
    private TMP_ColorGradient hoverGradient;

    public void OnPointerEnter(PointerEventData eventData)
    {
        OnHover();
    }

    public void OnPointerExit(PointerEventData eventData)
    {
        OnExit();
    }

    private void Start()
    {
        normalGradient = new TMP_ColorGradient(normalColor1, normalColor2);
        hoverGradient = new TMP_ColorGradient(hoverColor1, hoverColor2);
        text.color = normalGradient.topLeft;
    }

    public void OnHover()
    {
        text.color = hoverGradient.topLeft;
    }

    public void OnExit()
    {
        text.color = normalGradient.topLeft;
    }*/
}